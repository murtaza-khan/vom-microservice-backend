type ErrorPayload {
	field: String
	message: [String]
}

type PageInfo {
  startCursor: String!
  endCursor: String!
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
}

type ResponseType {
  status: Boolean!
  message: String!
}
